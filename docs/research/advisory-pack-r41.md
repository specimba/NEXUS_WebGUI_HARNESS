# Advisory Pack Inventory — r41

**Task ID:** r41-b · **Date:** 2026-09-24
**Source:** `/home/z/advisory-inbox/` (Google Drive pack, `manifest.tsv` = 14 files; `_listing.html` is Drive housekeeping, not part of the pack).
**Ground truth:** ADVISORY-B read in full; every other file head-sampled + shape-verified (jq/rg) this task. The 113 MB tar backup is extracted at `/home/z/restore-staging` — reference only, DO NOT modify.

> Secrets note: the vault, the export, and the Hyperbrowser guide all contain REAL credentials. They stay in `/home/z/advisory-inbox/` (git-ignored territory); no key material is copied into any repo file. BYOK doctrine applies — these belong in the user's browser vault, not in code.

---

## 1. The 14 files

| # | File | Size | Contents | Actionable for this project |
|---|------|------|----------|-----------------------------|
| 1 | `zaiGLM53flashWEBGUIforNEXUScreationADVISORY-B.txt` | 14 KB / 508 ln | "NEXUS Coordination & Delegation Advisory v1" — 11 sections: purpose, operating principles, agent delegation model (Orchestrator/Builder/Verifier/Researcher/Stabilizer/Archivist), round budgeting S/M/L, verification + anti-theatre rules, cron & automation policy (boot-ensure, tiers, locks, recovery), harness selection policy, memory & context policy, failure handling, handoff template, roadmap P0–P2. Closing recommendation: turn it into three repo artifacts. | **Done r41-b** → `docs/OPERATING_DOCTRINE.md`, `docs/ROUND_HANDOFF_TEMPLATE.md`, `ops/cron.jobs.json` (adapted to shipped r26–r40 reality). Re-read §4/§6 whenever a claim or schedule is disputed. |
| 2 | `...ADVISORY-A.txt` | 207 KB / 3,085 ln | Longest chat-log synthesis: the raw conversation where the user demands (a) cron reactivation at restrictive 30m/1h/6h/12h cadences ("exec limits exceeded" root cause), (b) harness-selection capabilities (vs the "PraisonAI 🦞 24/7 AI Workforce" marketing line), (c) anti-"progression theatre" coordination for developer agents. | Requirements ledger for r34 (harness presets) + r34-a/r37 (automation visibility). Keep as the source of the user's own wording when judging whether a delivered feature matches the demand. |
| 3 | `...ADVISORY-C.txt` | 85 KB / 1,370 ln | Second chat-log synthesis: thematic advisory grounded in the r28→r33 chronology; names the full cron-evaporation history (jobs 402913/408563/408717/408812/408899), free-frontier provider inventory, relay doctrine, backup/rollback + fork-divergence warnings, BYOK. | Historical evidence for `ops/cron.jobs.json` `platformConstraints.execLimitEvidence`; the provider inventory matches the r30–r32 landed roster. |
| 4 | `HYPERBROWSERandJEVfullGUIDE.txt` | 210 KB / 7,773 ln | Hyperbrowser account + docs walkthrough (cloud headless browsers: sessions, web/scrape/task endpoints, extensions, live API-call logs) + JEV guide material. Opens with a live `hb_…` API key — secret. | **r41-a:** REST `POST https://api.hyperbrowser.ai/api/scrape` (probe-verified keyless → 401) as a BYOK server tool; Hyperbrowser MCP is stdio-only (`npx hyperbrowser-mcp`) → does NOT fit the r38 stateless client, REST is the path. |
| 5 | `BRIGHTDATAscrapperGUIDEexpert.txt` | 82 KB / 2,402 ln | Bright Data account capture: 5,000 free credits (renew 01.10.26), per-tool credit costs (Web Unlocker/SERP 1:1, Browser API 5 credits/MB, Scrapers 1/record) + MCP configuration pages. | **r41-a:** remote MCP endpoint `https://mcp.brightdata.com/mcp?token=KEY` fits the r38 browser-direct stateless client directly → preset quick-add (token = BYOK header/param). Credit metering argues for per-tool gating, already our doctrine. |
| 6 | `researchPODguide.txt` | 53 KB / 442 ln | researchpod.app UI capture: paper search across AI&CS/Biology/Physics categories, library/media/add-paper flows, account panel. | Candidate source for the Radar Papers tab ecosystem + the user's research workflow. No API in the capture — S-size recon task before any integration; queue, do not build blind. |
| 7 | `fancyLINKSandUSEFULtoolsSITESguide2409.txt` | 468 KB / 11,557 ln | Curated link dump of useful sites/tools (opens with jakecuth.com portfolio/lab index; hundreds of entries). | Mining source for Radar/discovery candidates and future research rounds; an index task, not a build spec. |
| 8 | `devtools_need_all_of_that_names_of_hobby_tagged_models_in_th.md` | 34 KB / 321 ln | Chrome DevTools AI-assistance chat export (2026-09-23): extracting every "hobby"-tagged model name + provider from a roster UI (zinc-chip selectors). | Feature signal for the free-model tracker: users want tag/class-based filtering ("hobby" tier). Also a candidate tracker-source classification flag. |
| 9 | `178x3onxsxg3m.js` | 302 KB | Turbopack bundle chunk containing a hardcoded model catalog: **313 entries** `{id, name, provider, providerHumanName, makerHumanName, modelApiName, supportsStructuredOutput, supportsToolCalling, gateway, info.description}` across bedrock(29)/openai(28)/groq(24)/fireworks(23)/google(22)/mistral(19)/perplexity(17)/anthropic(15)/xai(12)/deepinfra(12)/azure(12)/novita(11)…; exactly one `"hobby"` tag reference. | Reference model catalog for the tracker: adopt the capability-flag shape (`supportsToolCalling`/`supportsStructuredOutput`) in tracker normalization; use as an independent cross-check roster for free-lane identification. |
| 10 | `praison-provider-vault.json` | 2.7 KB | Vault slice `{kind:"praison-provider-vault", exportedAt 2026-09-23, providerKeys(17), activeProviderId, defaultModel, provider}` — real keys for vyce, pollinations, openrouter, groq, kilo (JWT), opencode (`oc_sk_…`, muse-spark-1.3-contributor-free), aihubmix, etc. | The r32-B unlock: opencode + kilo keys arm those gateways/relay hops in the user's browser vault. **Not importable by current Settings import** (shape lacks the required arrays — see §3); needs the dedicated vault-import path (lead's r41 item). |
| 11 | `praisonai-export.json` | 335 KB | Full app-state export in `handleExport` shape: `exportedAt` + `settings` (providerKeys ×17, relay on/off + order, defaults) + agents (5) + conversations (5) + workflows (3). No `suites` key (r37-compatible import handles that). | Restorable app state: migration between profiles, QA seeding, incident recovery. Imports cleanly with current code; **`settings.providerKeys` wholesale-overwrites the local vault on import** (see §3). |
| 12 | `...creationlogs1.txt` | 415 KB / 9,965 ln | Raw creation log #1 — project genesis: "clone specimba/PraisonAI as an app" directive, GLM-5.3-Flash session, actions/skill traces. | Provenance/archaeology only; supersedes nothing in the repo (worklog is canonical). |
| 13 | `...creationlogs2.txt` | 97 KB | Raw creation log #2 — later app-state capture (nav, Model Tracker ticker w/ `ling-3.0-flash-vl:free`, Vyce provider badge). | Provenance only. |
| 14 | `NEXUS_praison_WEBGUI_v1.tar` | 108 MB | Full workspace backup; extracted at `/home/z/restore-staging` (complete git history incl. `fork/NEXUS_WebGUI_HARNESS` branch + rollback tags r25–r31). | Disaster-recovery reference + git-forensics source (already used r31-1 to recover the DSH upload file). Do NOT modify; origin remains the canonical backup. |

## 2. Cross-cutting read

- The three advisories + two logs are ONE user voice in three passes: *sustainable automation → harness selection → verifiable, non-theatrical delivery*. Everything actionable from that voice has since landed (r34 harness, r34/r37 automation visibility, r38 MCP, r39/r40 bake-off adoption + gates) — the pack's remaining NEW material is the tooling/geodata: Bright Data remote MCP, Hyperbrowser REST scrape, provider keys, tracker catalog reference.
- Integration order implied by the pack: (1) Bright Data MCP preset (S — fits r38 client, token=BYOK), (2) Hyperbrowser REST scrape tool (S–M — server tool + provider key + guardPublicUrl not needed for the API host), (3) vault-import path in Settings (M — see §3), (4) tracker capability flags from the JS catalog schema (S), (5) researchPOD/fancyLINKS as Radar source-candidates backlog.

## 3. Import-path verification (current `settings-view.tsx` code, read-only analysis)

- `handleImportFile` accepts ONE `.json` file, requires `agents`, `conversations`, `workflows` arrays, then writes persist-shaped slices for `praison-agents/-conversations/-workflows/-settings` (`suites` only when present) and reloads.
- `praisonai-export.json` → imports successfully; `settings` is shallow-merged over `DEFAULT_SETTINGS`, so **the bundle's `providerKeys` object replaces the local vault wholesale** (hydration preseed then only fills providers left keyless).
- `praison-provider-vault.json` → **rejected** ("Invalid export file"): no `agents/conversations/workflows` arrays; the importer has no `kind` discrimination. A vault-import path must be added before these 17 keys can arm a profile from a file.

## 4. Solved incident (record for the anti-theatre ledger)

**hermes-agent notification spam (user ask: "fix that repo actions giving me mail spams constantly") — FIXED, commit `0420879`.**
Root cause chain: `specimba/hermes-agent` (rehost) ships ZERO `v*` release tags → scheduled `install-e2e.yml` (every 12h) runs `scripts/sandbox/pick-release-tags.sh`, which intentionally exits 1 on "no release tags" → pick-releases fails in 2s → update/installer skip → the RUN FAILS → GitHub emails the owner every 12h (168 failed runs on record). Fix: pick step degrades to an empty matrix + a `has_tags` output; update/installer jobs are gated on `has_tags == 'true'` → next scheduled run goes green-skipped and the spam stops. OSV-Scanner's failure was deliberately left alone (legitimate vulnerability signal; its logs had expired for diagnosis).
